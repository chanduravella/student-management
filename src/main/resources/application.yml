server:
  port: 8080
  servlet:
    context-path: /student-management

spring:
  profiles:
    active: default
  datasource:
    url: jdbc:postgresql://localhost:5432/studentdb
    username: postgres
    password: 0000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true